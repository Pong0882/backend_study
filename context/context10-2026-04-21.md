# Context 10 — 2026-04-21

## 오늘 한 것

### day12 (2026-04-21) — CORS 설정 + 이론 정리 + Docker 배포 환경 구성 (Issue #46)

**구현 (이전 커밋):**
- `SecurityConfig.java` — `CorsConfigurationSource` 빈 추가, `.cors()` 적용
  - 허용 Origin: `https://pongtrader.pro`, `https://api.pongtrader.pro`, `http://localhost:5173`, `http://localhost:3000`
  - `allowCredentials(true)` — JWT Authorization 헤더 포함 요청 허용
- `application-prod.yml` — `springdoc.server-url` 환경변수화 (`${SPRINGDOC_SERVER_URL}`)

**Docker 배포 환경 구성:**
- `frontend/Dockerfile` — Node 빌드 + Nginx 서빙 2단계 빌드
- `frontend/docker-compose.yml` — 프론트 전용 (서버 분리 대비), `pong-network` external 참조
- `frontend/nginx-spa.conf` — React Router history fallback
- `frontend/src/api/axios.js` — `VITE_API_BASE_URL` 환경변수로 baseURL 결정 (로컬/운영 분리)
- `nginx/pongtrader.conf` — `pongtrader.pro` → frontend:80, `api.pongtrader.pro` → app:8080
- `nginx/docker-compose.yml` — Nginx 컨테이너 전용, `pong-network` external 참조
- `docker-compose.yml` — `SPRINGDOC_SERVER_URL` 추가, `pong-network` external 전환
- `.env.example` — `SPRINGDOC_SERVER_URL` 항목 추가

**이론 정리:**
- `notes/phase-14-security-jwt/cors.md` — SOP 원리, Preflight, Spring Security CORS 설정, Vite 프록시 우회
- ROADMAP Phase 14-3 실습 항목 체크

**devlog/velog:**
- `devlog/day11-2026-04-20.md` — CORS 트러블슈팅 섹션 추가
- `devlog/velog/day11-23-트러블슈팅-cors-swagger-subdomain.md` — velog 초안 작성

---

## 현재 상태

- `pongtrader.pro` 외부 접속 가능
- Spring Boot + MySQL + Redis Docker로 실행 중 (pong-server VM)
- Cloudflare 터널 상시 실행 중 (systemd)
- `api.pongtrader.pro` — Cloudflare DNS + 터널 인그레스 설정 완료, 백엔드 접속 확인
- **프론트: 로컬 개발 중 (미배포) — feat/issue-45-frontend 브랜치**
- **Issue #46 진행 중 — feat/issue-46-api-subdomain 브랜치 (커밋 전)**

## VM 구성

```
172.24.xxx.xxx  — Windows 호스트
172.24.xxx.xxx  — pong-server  (Spring Boot + MySQL + Redis + Promtail, 4GB)
172.24.xxx.xxx  — monitoring-server (Prometheus + Grafana + Loki, 1.5GB)
172.24.xxx.xxx  — k6-server (부하 테스트 전용, 1GB)
```

## 배포 구조 계획 (미완성 — Issue #46, #47)

```
pongtrader.pro         → Nginx → React 빌드 결과물 (Docker)
api.pongtrader.pro     → Nginx → Spring Boot :8080 (Docker)
grafana.pongtrader.pro → Grafana (현재 그대로)
```

- VM 추가 없이 pong-server 하나에서 Nginx가 두 도메인 처리

---

## 오늘 완료한 항목

- [x] SecurityConfig CORS 설정 추가 (`localhost:3000` 포함)
- [x] CORS 이론 정리 → [notes](../notes/phase-14-security-jwt/cors.md)
- [x] Swagger server-url 환경변수화
- [x] 프론트엔드 Docker 환경 구성 (Dockerfile + docker-compose + nginx-spa.conf)
- [x] Nginx Docker 구성 (두 서브도메인 라우팅)
- [x] pong-network external 네트워크로 전환 (백엔드/프론트/nginx 분리 관리)

---

## 결정한 것들

- [2026-04-21] CORS `allowCredentials(true)` 사용 시 와일드카드 Origin 불가 — 명시적 4개 Origin으로 관리
- [2026-04-21] Spring Security 필터 체인 안에서 `.cors()` 연결 — `WebMvcConfigurer`만으로는 Security 필터가 먼저 막음
- [2026-04-21] 프론트/백엔드/Nginx docker-compose 파일 분리 관리 — 서버 분리 시 프론트만 이동 가능하도록
- [2026-04-21] `pong-network`를 external 네트워크로 전환 — 여러 compose 파일이 같은 네트워크 공유하기 위함 (`docker network create pong-network` 최초 1회 필요)

---

## 다음에 할 것

- **feat/issue-46-api-subdomain PR 머지**
- Issue #47 — 프론트엔드 CI/CD (GitHub Actions + 서버 배포 자동화)
- Issue #48 — GET /api/stocks/{code}/prices 페이지네이션 + k6 성능 비교
- Issue #49 — 증권사 계좌 등록 시 KIS API 유효성 검증
- Issue #32 — KIS 실제 주문 전송 연동
- Issue #33 — 포트폴리오 평가손익 계산
- 서버 타임존 UTC → KST 수정
- DB stock_prices 종목코드 앞자리 0 손실 데이터 재삽입 (DELETE 후 import_stock_csv.py 재실행)
