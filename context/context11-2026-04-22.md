# Context 11 — 2026-04-22

## 오늘 한 것

### Issue #48 — 일봉 테이블 페이지네이션 + 차트 엔드포인트 분리 ✅

**원인 분석:**
- 렉 원인이 서버가 아닌 브라우저 렌더링임을 확인
- Network 탭: 서버 응답 281ms / 59KB → 빠름
- Performance 탭: Main thread 822ms 블로킹
- 차트 주석처리 테스트 → 렉 사라짐 → 차트 destroy/재생성 + DOM 18,000개 노드 생성이 원인

**구현:**
- `StockPriceRepository` — `Page<StockPrice> findByStockOrderByTradeDateDesc(Stock, Pageable)` 추가
- `StockService` — `getDailyPrices()` 페이지네이션 / `getAllDailyPrices()` 차트용 분리
- `StockController` — `/prices?page=0&size=50` + `/prices/chart` 엔드포인트 분리
- `Stocks.jsx` — `chartPrices`/`prices` state 분리, 테이블 페이지 버튼 추가

**결과:**
- 테이블 응답: 59.2KB → 7.9KB (-87%), 281ms → 26ms (-91%)
- 종목 변경 시 렉 사라짐

**기록:**
- devlog/day13-2026-04-22.md 작성
- devlog/velog/day13-26-트러블슈팅-stocks-페이지-렉-브라우저-렌더링.md 작성
- PR 머지, 이슈 닫음

---

### Issue #49, #58 — 증권사 계좌 KIS API 검증 + 활성화 토글 ✅ (커밋 완료, PR 대기)

**완료:**
- `ErrorCode.BROKER_ACCOUNT_INVALID_KEY` (400), `BROKER_ACCOUNT_KIS_RATE_LIMIT` (429) 추가
- `BrokerAccountService.validateKisApiKey()` — KIS rate limit(EGW00133) 분기 처리
- `BrokerAccount.activate()` + `BrokerAccountService.activate()` 추가
- `PATCH /api/broker-accounts/{id}/activate` 엔드포인트 추가
- `BrokerAccounts.jsx` — `a.active` → `a.isActive` 수정, 활성화/비활성화 토글 버튼

**커밋:** `feat/issue-49-58-kis-api-validation` 브랜치 푸시 완료

---

### Issue #34 — KIS 주문 전송 연동 1단계 (블로킹 🚫)

**완료:**
- `Order` — `kisOrderNo` 필드 + `assignKisOrderNo()` 추가
- `BrokerAccount` — `accountNumber` 필드 추가
- `BrokerAccountCreateRequest` — `accountNumber` 추가
- `KisOrderResponse` DTO 신규 생성
- `KisOrderService` 신규 생성 → `KisAuthService` 공용 토큰 재사용으로 단순화
- `OrderService` — KIS 주문 전송 연결 (1단계: 동기, 단일 트랜잭션)
- `OrderResponse` — `kisOrderNo` 추가
- `ErrorCode.KIS_ORDER_FAILED` 추가
- `BrokerAccounts.jsx` — 계좌번호 입력 필드 추가
- TR ID 신버전 교체 (`VTTC0012U`), `custtype: P` 헤더 추가, `EXCG_ID_DVSN_CD: KRX` 추가

**현재 에러:**
```
EGW00356 — 타겟 서버가 올바르지 않습니다
```
모든 파라미터(URL, TR ID, appkey, appsecret, token, body) 정상 확인.
curl 직접 요청도 동일 에러. **KIS 서버 측 버그로 추정. 고객센터 정식 문의 필요.**

**주의:** 디버깅 과정에서 appkey/appsecret/token이 KIS 문의 채널에 노출됨 → **KIS Developers에서 APP Secret 갱신 필요**

**남은 것:**
- KIS Developers APP Secret 갱신 → `.env` + DB 업데이트
- KIS 고객센터(1544-5000) 정식 문의
- 버그 수정 확인 후 커밋/PR

---

### Issue #60 — 시세 페이지 현재가 폴링 (진행 중 🔧)

**완료:**
- `Stocks.jsx` — 장 운영 시간(평일 09:00~15:30) 10초마다 현재가 자동 갱신
- 종목 변경 시 이전 타이머 `clearInterval`로 정리
- 현재가 카드에 `● 10초 갱신 중` / `● 장 마감` 표시

**남은 것:**
- 빌드/커밋/PR

---

### 이슈 등록

- 장 중 현재가 Redis 캐싱 + 장 마감 일봉 자동 수집 이슈 등록
- KIS 주문 비동기 처리 및 WebSocket 체결 통보 이슈 등록
- HikariCP 커넥션 풀 사이즈 튜닝 이슈 등록
- 실서비스 전환 전 API 키 보안 강화 이슈 등록

---

## 현재 상태

- `pongtrader.pro` 외부 접속 가능
- Spring Boot + MySQL + Redis Docker로 실행 중 (Windows 로컬)
- Issue #34 진행 중 — 로컬에서 작업, 커밋 전
- KIS IP 등록 필요 — 주문 테스트 전

## VM 구성

```
172.24.xxx.xxx  — Windows 호스트
172.24.xxx.xxx  — pong-server  (Spring Boot + MySQL + Redis + Promtail, 4GB)
172.24.xxx.xxx  — monitoring-server (Prometheus + Grafana + Loki, 1.5GB)
172.24.xxx.xxx  — k6-server (부하 테스트 전용, 1GB)
```

## 배포 구조

```
pongtrader.pro         → Nginx → React 빌드 결과물 (Docker)
api.pongtrader.pro     → Nginx → Spring Boot :8080 (Docker)
grafana.pongtrader.pro → Grafana (monitoring-server VM)
```

---

## 오늘 완료한 항목

- [x] Issue #48 — 일봉 테이블 페이지네이션 + 차트 엔드포인트 분리
- [x] docker-compose.yml ENCRYPT_SECRET_KEY 누락 버그 수정
- [x] Issue #49, #58 — KIS API 검증 + rate limit 처리 + 계좌 활성화 토글
- [x] Issue #60 — 시세 페이지 현재가 폴링 구현

---

## 결정한 것들

- [2026-04-22] 차트는 페이지네이션 대신 전체 데이터 별도 엔드포인트 분리 — 차트는 전체 데이터가 필요하고, 페이지네이션 적용 시 휠 스크롤 UX가 깨짐
- [2026-04-22] 차트 lazy loading은 별도 이슈로 분리 — 구현 복잡도가 있어 우선순위 낮게 관리
- [2026-04-22] KIS API 키 검증 시 MOCK/REAL base URL 분리 — 모의투자와 실전투자 엔드포인트가 다름
- [2026-04-22] 주문용 토큰은 KisAuthService(공용) 대신 계좌 appkey/appsecret으로 직접 발급 — KIS는 토큰 발급자와 주문 appkey가 일치해야 함
- [2026-04-22] 주문 처리 1단계는 동기 + 단일 트랜잭션으로 단순 구현 — 직접 에러 만나면서 단계적 개선 예정
- [2026-04-22] 현재가 폴링은 장 운영 시간(09:00~15:30)에만 동작 — 추후 KIS WebSocket으로 교체 예정

---

## 다음에 할 것

- **APP Secret 갱신** — KIS Developers에서 모의투자 앱 APP Secret 갱신 → `.env` + DB 업데이트 (노출 대응)
- **KIS 고객센터 문의** — 1544-5000, EGW00356 버그 신고
- **Issue #34 마무리** — KIS 버그 수정 후 주문 테스트 → 커밋/PR
- **Issue #49, #58 PR** — 브랜치 푸시 완료, PR 생성 필요
- **Issue #60 커밋/PR** — 폴링 구현 완료, 커밋 필요
- 차트 lazy loading 이슈 (별도, 우선순위 낮음)
- Issue #33 — 포트폴리오 평가손익 계산
- 서버 타임존 UTC → KST 수정
- DB stock_prices 종목코드 앞자리 0 손실 데이터 재삽입
