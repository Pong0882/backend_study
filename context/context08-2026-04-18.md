# Context 08 — 2026-04-18

## 오늘 한 것

### day10 (2026-04-18) — KisAuthService Redis 캐싱 + TDD 첫 적용 + 버그 수정 2개

**구현 (Issue #17):**
- KisAuthService 메모리 캐싱 → Redis 캐싱 전환
- AppConfig에 RestClient Bean 추가 (테스트 Mock 주입 가능하도록)
- ErrorCode에 KIS_AUTH_FAILED 추가
- build.gradle 의존성 정리 (spring-boot-starter-test 통합, junit-platform-launcher 추가)

**TDD 첫 적용:**
- KisAuthServiceTest 작성 — 캐시 히트 / 캐시 미스 / KIS API 실패 3개
- Red 확인 (구현 망가뜨려서 3개 실패 확인) → Green 확인 (3개 통과)
- RestClient 체이닝 Mock 방법 체득

**이론 정리:**
- notes/phase-10-tdd/tdd-basics.md
- notes/phase-16-redis/redis-cache-pattern.md
- notes/phase-6-java-jvm/java-time-api.md
- notes/phase-10-tdd/mockito-verify.md
- notes/phase-10-tdd/mockito-strictness.md

**버그 수정:**
- Issue #22 — `@SQLRestriction("deleted_at IS NULL")` User Entity에 적용, 탈퇴 유저 노출 차단
- Issue #23 — `AuthService.signUp()`에 Portfolio 자동 생성 + `@Transactional` 추가

---

## 현재 상태

- `pongtrader.pro` 로 외부 접속 가능한 상태
- Spring Boot + MySQL + Redis Docker로 실행 중 (pong-server VM 안)
- Cloudflare 터널 상시 실행 중 (systemd)
- Grafana: `grafana.pongtrader.pro` 접속 가능
- **브랜치: feat/issue-31-kis-auth-redis-tdd — 미머지 상태**
- **IntelliJ Build and run using → IntelliJ IDEA로 설정 (Gradle 아님)**

## VM 구성

```
192.168.100.1   — Windows 호스트
192.168.100.10  — pong-server  (Spring Boot + MySQL + Redis + Promtail, 4GB)
192.168.100.20  — monitoring-server (Prometheus + Grafana + Loki, 1.5GB)
192.168.100.66  — k6-server (부하 테스트 전용, 1GB)
```

---

## 현재 구현된 API

| 메서드 | 경로 | 설명 | 인증 필요 |
|--------|------|------|----------|
| POST | /api/auth/signup | 회원가입 | X |
| POST | /api/auth/login | 로그인 (토큰 발급) | X |
| POST | /api/auth/refresh | Access Token 재발급 | X |
| POST | /api/auth/logout | 로그아웃 (Refresh Token 삭제) | O |
| GET | /api/stocks/{code} | 현재가 조회 (KIS 실시간) | X |
| GET | /api/stocks/{code}/prices | DB 저장된 일봉 데이터 조회 | X |
| POST | /api/stocks/{code}/fetch | 기간별 일봉 데이터 수집 → DB 저장 | X |
| POST | /api/broker-accounts | 증권사 계좌 등록 | O |
| GET | /api/broker-accounts | 내 계좌 목록 조회 | O |
| GET | /api/broker-accounts/{id} | 계좌 단건 조회 | O |
| DELETE | /api/broker-accounts/{id} | 계좌 비활성화 | O |
| POST | /api/watchlist | 관심 종목 등록 | O |
| GET | /api/watchlist | 내 관심 종목 목록 조회 | O |
| PATCH | /api/watchlist/{id} | 알림가 수정 | O |
| DELETE | /api/watchlist/{id} | 관심 종목 삭제 | O |
| POST | /api/strategies | 전략 생성 | O |
| GET | /api/strategies | 내 전략 목록 조회 | O |
| GET | /api/strategies/{id} | 전략 단건 조회 | O |
| PATCH | /api/strategies/{id}/activate | 전략 활성화 | O |
| PATCH | /api/strategies/{id}/pause | 전략 일시정지 | O |
| PATCH | /api/strategies/{id}/deactivate | 전략 중지 | O |
| DELETE | /api/strategies/{id} | 전략 삭제 (ACTIVE 불가) | O |
| POST | /api/orders | 수동 주문 생성 | O |
| GET | /api/orders | 내 주문 목록 조회 | O |
| GET | /api/orders/{id} | 주문 단건 조회 | O |
| PATCH | /api/orders/{id}/cancel | 주문 취소 (PENDING만) | O |
| GET | /api/portfolio | 내 포트폴리오 조회 | O |
| PATCH | /api/portfolio/holdings/{id}/toggle-hidden | 보유 종목 숨김 토글 | O |

---

## 오늘 완료한 항목

### Phase 10 — TDD
- [x] TDD 첫 적용 — KisAuthServiceTest (캐시 히트/미스/실패 3개)
- [x] Red → Green 사이클 완주
- [x] 이론 정리: tdd-basics.md, redis-cache-pattern.md

### 사전 수정
- [x] DTO 유효성 검증 추가 (spring-boot-starter-validation, @Valid, MethodArgumentNotValidException 핸들러)
- [x] StockService RestClient Bean 주입 전환 + @RequiredArgsConstructor

### Issue #26~#30 도메인 API 구현
- [x] #26 BrokerAccount API — 등록/목록/단건/비활성화
- [x] #27 Watchlist API — 등록/목록/알림가수정/삭제
- [x] #28 Strategy API — 생성/목록/단건/활성화/일시정지/중지/삭제
- [x] #29 Order API — 수동주문/목록/단건/취소
- [x] #30 Portfolio API — 조회/숨김토글
- [x] PROJECT.md API 섹션 업데이트 (프론트 참고용)

---

## 결정한 것들

- [2026-04-18] IntelliJ 빌드 설정 — `Build and run using: IntelliJ IDEA` (Gradle 모드에서 junit-platform-launcher 문제로 전환)
- [2026-04-18] build.gradle `outputs.upToDateWhen { false }` 유지 — Gradle test 태스크 UP-TO-DATE 스킵 방지

---

## 결정한 것들

- [2026-04-18] 최적화 포인트는 즉시 구현 않고 TODO 주석 → GitHub 이슈로 관리
- [2026-04-18] 이슈 #20(appkey 암호화), #19(N+1), #24(트랜잭션), #21(JSON 검증) 기존 이슈 재활용
- [2026-04-18] 신규 이슈: #32 KIS 주문 전송 연동, #33 평가손익 계산

## 다음에 할 것

- feat/issue-26-30-domain-apis → main PR 머지
- Issue #32 KIS API 실제 주문 전송 연동
- Issue #33 포트폴리오 평가손익 계산
- 서버 타임존 UTC → KST 수정
- 인덱스 실습 (stock_prices 대량 적재 후 EXPLAIN 비교)
- HikariCP 커넥션풀 튜닝 (maximumPoolSize 조정 후 성능 비교)
