# Context 03 — 2026-04-09

## 오늘 한 것

### Spring 개발 (day03)
- 브랜치 전략 단순화 실제 적용 (develop → main 머지, develop 삭제)
- 메인 페이지 워런 버핏 명언 추가
- Docker 컨테이너 내부 DNS 트러블슈팅 (daemon.json Google DNS 설정)
- Spring Security + JWT 구현 (회원가입 / 로그인 / Access Token + Refresh Token 발급)
- JWT 시크릿 키 보안 분리 (application-local.yml gitignore, prod 환경변수)
- Refresh Token DB 저장 구현 (재발급 / 로그아웃)
- @Slf4j 로깅 추가 (AuthService, JwtAuthenticationFilter)
- logback-spring.xml 환경별 로그 레벨 분리 (local=DEBUG, prod=WARN)
- Swagger JWT Bearer 인증 설정 (Authorize 버튼)
- Swagger API 문서 커스터마이징 (@Operation, @ApiResponse, @Schema 예시값)

### 이론 정리 (day03)
- Spring Security Filter Chain, SecurityContext, CORS, CSRF, @EnableWebSecurity
- JWT 구조 (Header/Payload/Signature), Access/Refresh Token 전략, DB 저장 전략
- BCrypt / PasswordEncoder
- JPA Entity 어노테이션 (@Entity, @Table, @Id, @GeneratedValue, @Column, @Enumerated)
- @NoArgsConstructor(PROTECTED), @Builder 패턴
- Spring Profiles / 민감 정보 관리 전략
- 로깅 전략 (@Slf4j, 로그 레벨, logback-spring.xml)
- JPA와 Hibernate (ddl-auto, show-sql)
- Swagger 커스터마이징 (JWT 인증, @Operation, @Schema)
- git restore --staged vs git reset HEAD

### Velog
- #5 Spring Security + JWT 딥다이브 초안 작성 (`devlog/velog/day03-05-spring-security-jwt-deep-dive.md`)

## 현재 상태

- `pongtrader.pro` 로 외부 접속 가능한 상태
- Spring Boot + MySQL Docker로 실행 중 (VM 안)
- Cloudflare 터널 상시 실행 중 (systemd)
- **컴퓨터 재시작 시 자동으로 서버 올라옴**
- IntelliJ Remote SSH로 VM 접속 가능
- VM IP는 재시작 시 변경됨 (`multipass list` 로 확인)
- 로컬 개발 서버 별도 실행 중 (IntelliJ에서 직접 실행)

## VM 구조

- VM 이름: `pong-server` (Multipass, Ubuntu 24.04, CPU 2, 메모리 4GB, 디스크 20GB)
- 프로젝트 경로: `/home/ubuntu/Backend-A-to-Z/pong-to-rich`
- SSH 키: `C:\ProgramData\Multipass\data\ssh-keys\id_rsa`
- 접속: `ssh -i "C:/ProgramData/Multipass/data/ssh-keys/id_rsa" ubuntu@{multipass list로 확인한 IP}`
- systemd 서비스:
  - `cloudflared` — Cloudflare 터널 (자동 실행)
  - `pong-compose` — docker compose up -d (자동 실행, Docker 뜬 후)

## 브랜치 전략

- `feat/YYYY-MM-DD` → `main` 직접 PR (develop 브랜치 미사용)
- HOW_WE_WORK.md, ROADMAP.md 등 관리 문서는 main 직접 커밋 가능

## 현재 구현된 API

| 메서드 | 경로 | 설명 | 인증 필요 |
|--------|------|------|----------|
| POST | /api/auth/signup | 회원가입 | X |
| POST | /api/auth/login | 로그인 (토큰 발급) | X |
| POST | /api/auth/refresh | Access Token 재발급 | X |
| POST | /api/auth/logout | 로그아웃 (Refresh Token 삭제) | O |

## 오늘 완료한 항목

### Phase 5 — Git
- [x] git restore --staged vs git reset HEAD → [git-basics.md](../notes/phase-5-git/git-basics.md)

### Phase 7 — Spring Core
- [x] Spring Profiles / 민감 정보 관리 전략 → [spring-profiles.md](../notes/phase-7-spring-core/spring-profiles.md)

### Phase 8 — REST API
- [x] Swagger JWT 인증 설정 / API 커스터마이징 → [swagger-springdoc.md](../notes/phase-8-rest-api/swagger-springdoc.md)

### Phase 9 — 로깅 전략
- [x] @Slf4j / 로그 레벨 / logback-spring.xml → [logging-strategy.md](../notes/phase-9-logging/logging-strategy.md)

### Phase 11 — Database / JPA
- [x] JPA Entity 어노테이션 → [jpa-entity-annotations.md](../notes/phase-11-database-jpa/jpa-entity-annotations.md)
- [x] JPA와 Hibernate / ddl-auto / show-sql → [jpa-hibernate.md](../notes/phase-11-database-jpa/jpa-hibernate.md)

### Phase 14 — Security / JWT
- [x] Spring Security Filter Chain / SecurityContext / CORS / CSRF → [spring-security.md](../notes/phase-14-security-jwt/spring-security.md)
- [x] JWT 구조 / Access Token / Refresh Token / DB 저장 전략 → [jwt.md](../notes/phase-14-security-jwt/jwt.md)
- [x] BCrypt / PasswordEncoder → [password-encoder.md](../notes/phase-14-security-jwt/password-encoder.md)

## 결정한 것들

- [2026-04-09] Refresh Token은 DB에 저장 (이후 Redis 전환 시 성능 비교 예정)
- [2026-04-09] Access Token 재발급 방식 — 방식 A (401 받고 클라이언트가 재시도)
- [2026-04-09] Velog 포스팅 유형 3가지 — 수행 요약 / 트러블슈팅 / 딥다이브 (HOW_WE_WORK 업데이트)
- [2026-04-09] show-sql: false — logback으로 제어하는 방식으로 통일

## 정리 필요 (notes/pending.md)

- Refresh Token 재발급 속도 측정 (DB vs Redis 성능 비교) 🔲
- Swagger 고도화 + AOP 로깅 (테이블 늘어난 후 진행) 🔲

## 다음에 할 것

### day04 우선순위
- GitHub Actions CI/CD 파이프라인 구성 (main push → 빌드 → SSH → VM 배포)
- 전역 예외 처리 (@RestControllerAdvice, ErrorCode Enum)

### 나중에
- Redis로 Refresh Token 전환 + DB vs Redis 성능 비교
- Swagger AOP 로깅 고도화
- VM 고정 IP 설정 (netplan)
- 미니게임 아이디어 — 랜덤으로 움직이는 가상 주식 게임
