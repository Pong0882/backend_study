server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # React Router history 모드: 없는 경로는 index.html로 fallback
    location / {
        try_files $uri $uri/ /index.html;
    }
}
